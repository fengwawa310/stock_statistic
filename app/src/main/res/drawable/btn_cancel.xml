<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 实心 -->
    <solid android:color="@color/white"/>
    <!-- 渐变 -->
    <gradient android:startColor="@color/line_split"
        android:endColor="@color/line_split"/>
    <!-- 描边 -->
    <stroke
        android:width="1dp"
        android:color="@color/line_split" />
    <!-- 圆角 -->
    <corners
        android:radius="5dp" />

</shape>
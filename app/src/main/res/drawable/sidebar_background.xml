<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 实心 -->
    <solid android:color="@color/txt_hint"/>
    <!-- 渐变 -->
    <gradient android:startColor="@color/txt_hint"
        android:endColor="@color/txt_hint"/>
    <!-- 描边 -->
    <stroke
        android:width="1dp"
        android:color="@color/txt_hint" />

</shape>